FINAL CAPSTONE - AI STRATEGY

Cornerstone Relief International (CRI)
Checked: 2026-08-10


1. STRATEGIC POSITION

CRI must get to the point of being AI-native but not AI-dependent. AI is an assistive
The operating layer isn't meant to replace accountable, it is for a nine-person IT team.
human judgment.

The building design provides for greater separation of low sensitivity productivity use and limited access.
beneficiary workloads. Public or general purpose AI services should never be given access to any data that would infringe on the privacy of end users.
The case notes, medical information and credentials, and donor payments for beneficiaries.
Other data that is limited or restricted.


2. TOOL THREAD - WHAT CRI USES AI FOR

CRI uses AI as a tool for:

- summarizing infrastructure and security telemetry;
- drafting incident hypotheses and runbook suggestions;
- helping administrators write and review scripts, IaC, and documentation;
- translating and summarizing public or approved internal material;
- natural-language search over approved policy and operational knowledge;
- assisting with ticket classification and repetitive service-desk work.

f you do not specifically say you can do something with AI content, the AI will not show it in this thread.
by the autonomy ladder. AI does not approve/reject a wire transfer, or change your identity
A risk assessment, a risk acceptance or a governance decision is made by or on a firewall.

3. WORKLOAD THREAD - WHAT CRI RUNS

The primary restricted workload is a private beneficiary case-note RAG service.

Only returns records which the authenticated user is already permitted to view.
All retrieved documents will be considered untrusted data as well as instructions. The
service is not arbitrarily externally exit and does not have the ability to email or export restricted.
content that has not been approved via a workflow.

Other planned workloads are:

- a small local embedding service;
- model and evaluation jobs;
- AI-assisted operations;
- approved external APIs for low-sensitivity productivity.

CRI does not train a foundation model.


4. MODEL TIERS

TIER A - SMALL / LOW-COST MODELS

Use for classification, formatting, routing, extraction, and simple summaries.

Prefer the smallest model that meets the measured quality target.


TIER B - PRIVATE RESTRICTED RAG MODEL

Candidate model:

Qwen/Qwen3-14B

Repository revision observed during final research:

40c0698

Target deployment:

private inference lane
INT4 serving

The short repository revision is a planning reference. Production promotion
must replace it with a full immutable repository revision or signed OCI digest.

Production also requires:

- measured evaluation;
- privacy review;
- security review;
- model card;
- datasheet;
- AIBOM;
- rollback target;
- named human approval.


TIER C - HIGH-CAPABILITY EXTERNAL REASONING MODELS

Use only through an approved enterprise API for public information,
low-sensitivity organizational data, or de-identified data.

Restricted beneficiary records are prohibited.


EMBEDDING TIER

Employ a small embedding model. Tenants and document ACLs need to be preserved.

Embeddings and vector indexes are considered to be derived sensitive data.


5. BUILD VS BUY VS SELF-HOST

BUY

CRI acquires commercially available capabilities which the team of nine people does not need to reinvent:
The following are some of the most popular features of secure identity and access management tooling:
For low-sensitivity workloads, external AI APIs are available.

BUILD

CRI only creates the value and control layer that is specific to the CRI: retrieval policies,
[Microsoft] connectors, evaluation suites, approval workflows, data classification,
Observability, and governance integrations.

SELF-HOST / PRIVATE

The private controlled inference lane at CRI maintains a beneficiary case-note RAG.

This decision is based on privacy, residency, egress control, auditing and more.
beneficiary trust. It is not made because the given 8xH100 cost model is not provided.
cheaper.

6. ACTUAL COST EVIDENCE

The cost script was executed on the lab VM and saved to:

output/estate-cost.txt
evidence/estate-cost.txt

Actual workload:

500M tokens/month
400M input
100M output

Actual API examples from the course snapshot:

claude-haiku-4-5:  $900/month
claude-sonnet-4-6: $2,700/month
gpt-5.5:           $5,000/month
gemini-3-flash:    $500/month

Illustrative reserved self-host node:

8xH100
$22,000/month
1.5B-token monthly capacity
$44.00/1M tokens at the 500M-token workload

All four computed API/self-host crossover points are more than the provided
The claimed 1.5B-token per month capacity of node.



7. STANDARDS POSITION

CRI treats durable interfaces as architecture and products as replaceable
implementations.

Preferred contracts include:

- OpenTelemetry for observability;
- OpenAI-compatible inference APIs;
- OCI artifacts for model distribution;
- MCP with least-privilege scopes for agent-to-tool access;
- standard Kubernetes resource APIs.


8. HUMAN JUDGMENT

The last architectural placement is a blend of hybrid and conservative.

Automatic observation and narrow reversible repair is provided by CRI. Data deletion,
changes in the security-boundary, model promotion, financial commitment, and sensitive changes in the model.
Control over identity operations is in the hands of named human operators.

This boundary is intentional as it's the data of vulnerable people that CRI serves.
cannot be used as an experiment.


SOURCE NOTE

The primary references are listed in sources.txt. The above cost data are
These are not up-to-date vendor quotes, but outputs from the model provided by the course.
