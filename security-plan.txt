FINAL CAPSTONE - SECURITY PLAN

Cornerstone Relief International (CRI)
Normal Tier

Threat map checked:
2026-08-10


1. TRADITIONAL SECURITY BASELINE

IDENTITY

All of CRI's privileged access uses federated identity and phishing-resistant MFA/passkeys.
To access resources and services, users must log in with a username and password.Access to resources and services requires users to provide credentials (username and password).
workload identities for services and agents, and quick joiners/movers/leavers
processing.


LEAST PRIVILEGE

All the data and tools needed are provided just to every human and AI agent.
current role.

The credentials of the agents are not passed on to humans or other agents.

For RAG retrieval, the authenticated user is utilized and document ACLs will be utilized.

SEGMENTATION

There are separate areas for security management, security enterprise applications and security donors.
and financial systems, beneficiary information, AI inferences and development.

The RAG endpoint of the beneficiary is not public.

By default, the data plane of its egresses are deny.

Only via an approved gateway with DLP policy: External AI APIs are accessed only through a gateway with a DLP policy.



2. DEEPFAKE-RESISTANT PAYMENT CONTROL

Not a single video call, voice call, email, chat message or AI-generated message can be replaced by any of the other methods.
It can be used to authorize itself a wire transfer or a change in bank details.

Finance must:

1. make a call on a known number which is already stored in a trusted directory;
2. check the identity of the requester off-line;
3. get 2nd human naming.

An inside number, QR code or link provided in the suspicious message is
Has never had to be used for verification.



3. BACKUP AND RANSOMWARE CONTROL

Access controls, and immutable off-site backups and least-privilege access control are applied to donor/beneficiary systems.
Backup credentials, tested restoration and documented RPO/RTO.

Backup control planes are also distinct from the normal administrator's credentials.


4. AI TRUST BOUNDARIES

BOUNDARY A - USER TO APPLICATION

Authenticate the user before retrieval.

Classify the data before choosing the model lane.


BOUNDARY B - RETRIEVAL TO MODEL

Retrieved webpages, tickets, documents, filenames, and case notes are
UNTRUSTED CONTENT.

They are wrapped as data and cannot grant tool authority or override system
policy.


BOUNDARY C - MODEL OR AGENT TO TOOLS

MCP and other tool access is allowlisted, least privilege, and identity-scoped.

High-impact tools are unavailable without an approval gate.

Tool descriptions and tool responses are also treated as untrusted input.


BOUNDARY D - AI TO EXTERNAL SYSTEMS

Restricted beneficiary RAG has no arbitrary outbound HTTP, email, or export
tool.

Approved exports require destination allowlists, DLP, audit logging, and human
approval.

This implements the Rule of Two against the lethal trifecta.


BOUNDARY E - OUTPUT TO DOWNSTREAM SYSTEM

LLM output is never directly executed as shell, SQL, HTML, policy, or payment
instructions.

Structured output is schema-validated.

Privileged actions require separate authorization.


5. OWASP LLM TOP 10 (2025) TO MITRE ATLAS DEFENSIVE CROSSWALK

This is a pedestrian crosswalk which is used for protection. The OWASP and MITRE ATLAS are two separate taxonomies,
That is, not all the rows are perfect one-to-one correspondences.


LLM01 - PROMPT INJECTION

MITRE ATLAS:

AML.T0051
LLM Prompt Injection

AML.T0051.001
Indirect

CRI risk:

Malicious text in a case note, ticket, webpage, or other retrieved source
changes the model's intended behavior.

Controls:

- untrusted-content boundary;
- least-privilege tools;
- Rule of Two;
- human gates.


LLM02 - SENSITIVE INFORMATION DISCLOSURE

MITRE ATLAS:

AML.T0085.000
RAG Databases

AML.T0086
Exfiltration via AI Agent Tool Invocation

CRI risk:

Beneficiary names, locations, medical information, or other sensitive data are
retrieved or exported to an unauthorized destination.

Controls:

- identity-scoped retrieval;
- DLP;
- encryption;
- no arbitrary egress;
- audit retrieval and exports.


LLM03 - SUPPLY CHAIN

MITRE ATLAS:

AML.T0010
AI Supply Chain Compromise

AML.T0110
AI Agent Tool Poisoning

CRI risk:

A compromised model, software package, OCI image, or MCP tool enters
production.

Controls:

- pinned versions/revisions;
- immutable digests;
- signatures;
- AIBOM;
- scanning;
- restricted registries;
- tool-definition review.


LLM04 - DATA AND MODEL POISONING

MITRE ATLAS:

AML.T0020
Poison Training Data

AML.T0070
RAG Poisoning

AML.T0099
AI Agent Tool Data Poisoning

CRI risk:

Poisoned content contaminates fine-tuning, retrieval, or agent decisions.

Controls:

- data provenance;
- quarantine;
- signed curated datasets;
- ingestion review;
- rebuild indexes from protected source data.


LLM05 - IMPROPER OUTPUT HANDLING

MITRE ATLAS:

AML.T0067
LLM Trusted Output Components Manipulation

CRI risk:

Model output becomes executable shell, SQL, HTML, or trusted workflow input.

Controls:

- schema validation;
- escaping;
- sanitization;
- separate authorization;
- never directly execute free-form model text.


LLM06 - EXCESSIVE AGENCY

MITRE ATLAS:

AML.T0053
AI Agent Tool Invocation

AML.T0086
Exfiltration via AI Agent Tool Invocation

CRI risk:

An agent performs a technically valid but unauthorized destructive,
security-sensitive, or external action.

Controls:

- autonomy ladder;
- minimum tool scope;
- one-action approval;
- blast-radius limits;
- verification;
- WORM audit.


LLM07 - SYSTEM PROMPT LEAKAGE

MITRE ATLAS:

AML.T0069.002
System Prompt

AML.T0084
Discover AI Agent Configuration

CRI risk:

An attacker learns hidden instructions, data sources, or available tools and
uses that knowledge to bypass controls.

Controls:

- do not store secrets in system prompts;
- minimize prompt privilege;
- separate credentials from agent configuration;
- protect manifests.


LLM08 - VECTOR AND EMBEDDING WEAKNESSES

MITRE ATLAS:

AML.T0070
RAG Poisoning

AML.T0085.000
RAG Databases

CRI risk:

Cross-user retrieval, poisoned embeddings, or unauthorized access to indexed
beneficiary data.

Controls:

- namespace isolation;
- document ACLs;
- provenance;
- retrieval filtering;
- encryption;
- rebuildable indexes.


LLM09 - MISINFORMATION

Closest relevant ATLAS entries:

AML.T0067
LLM Trusted Output Components Manipulation

AML.T0048
External Harms

This is not presented as a perfect one-to-one mapping.

CRI risk:

Fabricated operational, beneficiary, or financial information is accepted as
fact.

Controls:

- source citations;
- evidence display;
- human verification;
- no financial authority.


LLM10 - UNBOUNDED CONSUMPTION

MITRE ATLAS:

AML.T0029
Denial of AI Service

CRI risk:

Token loops, agent loops, or oversized requests exhaust capacity or budget.

Controls:

- per-user limits;
- per-request token limits;
- tool-call limits;
- concurrency controls;
- $200/day organizational cap;
- circuit breakers.


6. AI SECURITY TESTING

Each quarter, tests are performed that involve direct prompt injection, indirect RAG prompt injection,
The capacity to retrieve data from other users, malicious tool data, too much agency, output.
Failure to validation, cost-exhaustion scenarios.

Findings are converted to Tracking Remediation Tickets.

Because the "model" or "tool" is not or cannot be given to the security team to review.
system is AI-assisted.


7. ACCOUNTABILITY

Security control owner:
Sofia Alvarez

Beneficiary data-protection owner:
Grace Kim

Operational/accountable executive:
Maya Chen

No AI model or agent may accept organizational risk on behalf of those humans.


SOURCE NOTE

Suppose that the OWASP 2025 risk edition and MITRE ATLAS technique names/IDs listed here are used.Assume that the technique names/IDs from OWASP 2025 risk edition and MITRE ATLAS are used in this.
Final review was made to recheck crosswalk. Primary URLs are given in
sources.txt.